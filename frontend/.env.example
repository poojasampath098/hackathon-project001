# Production build is served from Vercel project environment variables (set in
# Vercel project settings -> Environment Variables -> Production). Do NOT commit
# real values. Local dev uses .env (gitignored).
VITE_GOOGLE_CLIENT_ID=